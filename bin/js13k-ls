#!/usr/bin/env node

var program = require('commander');
var fs = require('fs');
var path = require('path');

// Usage
program
  .usage(' ')
  .parse(process.argv);

// TODO(ooflorent): List all games into GAMES_DIR
